# Database Configuration
DATABASE_URL=postgresql://postgres:ragpassword@postgres:5432/ragdb

# Redis Configuration (optional)
REDIS_URL=redis://redis:6379/0
REDIS_MAX_CONNECTIONS=10

# OpenAI Configuration (required for RAG agent)
OPENAI_API_KEY= your-api-key-here

# OpenAI Model Settings
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Security Settings
SECRET_KEY=your-secret-key-change-in-production-jwt-minimum-32-chars
WEBHOOK_SECRET=webhook-secret-change-in-production

# Rate Limiting
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=1/minute

# Performance Settings
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20
DB_COMMAND_TIMEOUT=60
CACHE_TTL_SECONDS=300
RATE_LIMIT_REQUESTS=10

# RAG Configuration
RAG_TOP_K=5
RAG_SIMILARITY_THRESHOLD=0.7
RAG_MAX_TOKENS=4000

# Application Settings
LOG_LEVEL=INFO
DEBUG=false
HOST=0.0.0.0
PORT=8000

# Cache Configuration
CACHE_TTL_SECONDS=300
CACHE_MAX_QUERY_LENGTH=1000

# WebSocket Configuration
WEBSOCKET_MAX_CONNECTIONS=1000
WEBSOCKET_PING_INTERVAL=30

# Security Features
ENABLE_AUTHENTICATION=true
ENABLE_RATE_LIMITING=true
ENABLE_IP_BLOCKING=true

# Monitoring Configuration
ENABLE_METRICS=true
LOG_LEVEL=INFO

# CORS Settings (for production, restrict to specific domains)
CORS_ORIGINS=*

# Email Configuration
# SMTP Settings (optional - for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=abhaymallik5566@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# SendGrid Settings (alternative to SMTP)
SENDGRID_API_KEY=your-sendgrid-api-key

# Email From Settings
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
EMAIL_FROM_NAME=RAG System
EMAIL_NOTIFICATIONS_ENABLED=true 